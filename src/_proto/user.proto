syntax = "proto3";

package users;

service UserService {
  rpc RegisterUser (User) returns (Response) {}
}

message User {
  string username = 1;
  string email = 2;
  string password = 3;
}

message Response {
  string message = 1;
}
